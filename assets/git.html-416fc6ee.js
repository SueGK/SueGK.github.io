import{_ as n,Y as r,Z as s,$ as e,a0 as t,a1 as a,a2 as o,F as l}from"./framework-cd097da3.js";const d={},c=o(`<div class="language-console line-numbers-mode" data-ext="console"><pre class="language-console"><code>$ git add -h
usage: git add [&lt;options&gt;] [--] &lt;pathspec&gt;...

    -n, --dry-run         dry run
    -v, --verbose         be verbose

    -i, --interactive     interactive picking
    -p, --patch           select hunks interactively
    -e, --edit            edit current diff and apply
    -f, --force           allow adding otherwise ignored files
    -u, --update          update tracked files
    --renormalize         renormalize EOL of tracked files (implies -u)
    -N, --intent-to-add   record only the fact that the path will be added later
    -A, --all             add changes from all tracked and untracked files
    --ignore-removal      ignore paths removed in the working tree (same as --no-all)
    --refresh             don&#39;t add, only refresh the index
    --ignore-errors       just skip files which cannot be added because of errors
    --ignore-missing      check if - even missing - files are ignored in dry run
    --chmod (+|-)x        override the executable bit of the listed files
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-console line-numbers-mode" data-ext="console"><pre class="language-console"><code>mkdircd() { #Make path for each argument and cd into the last path mkdir -p &quot;$@&quot; &amp;&amp; cd &quot;$_&quot; }
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="git-init" tabindex="-1"><a class="header-anchor" href="#git-init" aria-hidden="true">#</a> git init</h2><p>Use the <code>git init</code> command to create a new, empty repository in the current directory.</p><h3 id="git-directory-contents" tabindex="-1"><a class="header-anchor" href="#git-directory-contents" aria-hidden="true">#</a> .Git Directory Contents</h3><p><em>We&#39;re about to take a look at the <code>.git</code> directory...it&#39;s not vital for this course, though, so don&#39;t worry about memorizing anything, it&#39;s here if you want to dig a little deeper into how Git works under the hood.</em></p><p>Here&#39;s a brief synopsis on each of the items in the <code>.git</code> directory:</p>`,7),h=e("strong",null,"config file",-1),g=e("em",null,"project specific",-1),m=e("br",null,null,-1),u={href:"https://git-scm.com/book/en/v2/Customizing-Git-Git-Configuration",target:"_blank",rel:"noopener noreferrer"},p=e("blockquote",null,[e("p",null,"Git looks for configuration values in the configuration file in the Git directory (.git/config) of whatever repository you’re currently using. These values are specific to that single repository.")],-1),f=e("p",null,"For example, let's say you set that the global configuration for Git uses your personal email address. If you want your work email to be used for a specific project rather than your personal email, that change would be added to this file.",-1),b=o("<li><p><strong>description file</strong> - this file is only used by the GitWeb program, so we can ignore it</p></li><li><p><strong>hooks directory</strong> - this is where we could place client-side or server-side scripts that we can use to hook into Git&#39;s different lifecycle events</p></li><li><p><strong>info directory</strong> - contains the global excludes file</p></li><li><p><strong>objects directory</strong> - this directory will store all of the commits we make</p></li><li><p><strong>refs directory</strong> - this directory holds pointers to commits (basically the &quot;branches&quot; and &quot;tags&quot;)</p></li>",5),v=e("p",null,[t(`Remember, other than the "hooks" directory, you shouldn't mess with pretty much any of the content in here. The "hooks" directory `),e("em",null,"can"),t(" be used to hook into different parts or events of Git's workflow, but that's a more advanced topic that we won't be getting into in this course.")],-1),w=e("h3",{id:"further-research",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#further-research","aria-hidden":"true"},"#"),t(" Further Research")],-1),y={href:"https://git-scm.com/book/en/v2/Git-Internals-Plumbing-and-Porcelain",target:"_blank",rel:"noopener noreferrer"},k={href:"https://git-scm.com/book/en/v2/Customizing-Git-Git-Hooks",target:"_blank",rel:"noopener noreferrer"},x=o(`<h2 id="git-clone" tabindex="-1"><a class="header-anchor" href="#git-clone" aria-hidden="true">#</a> git clone</h2><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git clone https://github.com/udacity/course-git-blog-project
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>rename clone repository, you can specify the new directory name as an additional argument:</p><div class="language-console line-numbers-mode" data-ext="console"><pre class="language-console"><code>$ git clone https://github.com/udacity/course-git-blog-project new-name
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><strong>Verify Terminal Location</strong> TIP: Now before you clone anything, make sure you are located in the correct directory on the command line. Cloning a project creates a new directory and places the cloned Git repository in it. The problem is that you can&#39;t have nested Git repositories. So make sure the terminal&#39;s current working directory isn&#39;t located in a Git repository. If your current working directory is not in your shell&#39;s prompt, type pwd to print the working directory.</p><h3 id="git-clone-recap" tabindex="-1"><a class="header-anchor" href="#git-clone-recap" aria-hidden="true">#</a> Git Clone Recap</h3><p>The <code>git clone</code> command is used to create an identical copy of an existing repository.</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git clone &lt;path-to-repository-to-clone&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This command:</p><ul><li>takes the path to an existing repository</li><li>by default will create a directory with the same name as the repository that&#39;s being cloned</li><li>can be given a second argument that will be used as the name of the directory</li><li>will create the new repository inside of the current working directory</li></ul><h3 id="helpful-links" tabindex="-1"><a class="header-anchor" href="#helpful-links" aria-hidden="true">#</a> Helpful Links</h3>`,11),_={href:"https://git-scm.com/book/en/v2/Git-Basics-Getting-a-Git-Repository#Cloning-an-Existing-Repository",target:"_blank",rel:"noopener noreferrer"},T={href:"https://git-scm.com/docs/git-clone",target:"_blank",rel:"noopener noreferrer"},G={href:"https://www.atlassian.com/git/tutorials/setting-up-a-repository",target:"_blank",rel:"noopener noreferrer"},q=o(`<h2 id="git-status" tabindex="-1"><a class="header-anchor" href="#git-status" aria-hidden="true">#</a> git status</h2><p>As you can see in the GIF above, running <code>git status</code> in the <code>course-git-blog-project</code> project produces the following output:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>On branch master
Your branch is up-to-date with &#39;origin/master&#39;.
nothing to commit, working directory clean
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="explanation" tabindex="-1"><a class="header-anchor" href="#explanation" aria-hidden="true">#</a> Explanation</h3><p>The output tells us two things:</p><ol><li><code>On branch master</code> – this tells us that Git is on the <code>master</code> branch. You&#39;ve got a description of a branch on your terms sheet so this is the &quot;master&quot; branch (which is the default branch). We&#39;ll be looking more at branches in lesson 5</li><li><code>Your branch is up-to-date with &#39;origin/master&#39;.</code> – Because <code>git clone</code> was used to copy this repository from another computer, this is telling us if our project is in sync with the one we copied from. We won&#39;t be dealing with the project on the other computer, so this line can be ignored.</li><li><code>nothing to commit, working directory clean</code> – this is saying that there are no pending changes.</li></ol><h3 id="helpful-links-1" tabindex="-1"><a class="header-anchor" href="#helpful-links-1" aria-hidden="true">#</a> Helpful Links</h3>`,7),R={href:"https://git-scm.com/book/en/v2/Git-Basics-Recording-Changes-to-the-Repository#Checking-the-Status-of-Your-Files",target:"_blank",rel:"noopener noreferrer"},A={href:"https://git-scm.com/docs/git-status",target:"_blank",rel:"noopener noreferrer"},E={href:"https://www.atlassian.com/git/tutorials/inspecting-a-repository/git-status",target:"_blank",rel:"noopener noreferrer"},j=o('<h2 id="git-log" tabindex="-1"><a class="header-anchor" href="#git-log" aria-hidden="true">#</a> git log</h2><p>By <em>default</em>, this command displays:</p><ul><li>the SHA</li><li>the author</li><li>the date</li><li>and the message</li></ul><h3 id="navigating-the-log" tabindex="-1"><a class="header-anchor" href="#navigating-the-log" aria-hidden="true">#</a> Navigating The Log</h3>',4),I={href:"https://en.wikipedia.org/wiki/Less_(Unix)",target:"_blank",rel:"noopener noreferrer"},H=o("<ul><li>to scroll <strong>down</strong>, press <ul><li><code>j</code> or  <code>↓</code> to move <em>down</em> one line at a time</li><li><code>d</code> to move by half the page screen</li><li><code>f</code> to move by a whole page screen</li></ul></li><li>to scroll <strong>up</strong>, press <ul><li><code>k</code> or <code>↑</code> to move <em>up</em> one line at a time</li><li><code>u</code> to move by half the page screen</li><li><code>b</code> to move by a whole page screen</li></ul></li><li>press <code>q</code> to <strong>quit</strong> out of the log (returns to the regular command prompt)</li></ul><p>What could we do here to not waste a lot of space and make the output smaller? We can use a <strong>flag</strong>.</p>",2),B={href:"https://www.udacity.com/course/linux-command-line-basics--ud595",target:"_blank",rel:"noopener noreferrer"},C=o('<h2 id="git-log-oneline" tabindex="-1"><a class="header-anchor" href="#git-log-oneline" aria-hidden="true">#</a> git log --oneline</h2><p>The <code>git log</code> command has a flag that can be used to alter how it displays the repository&#39;s information. That flag is <code>--oneline</code>: <img src="https://testksj.oss-cn-beijing.aliyuncs.com/uPic/kmISzO.png" alt="kmISzO" loading="lazy"> This command:</p><ul><li>lists one commit per line</li><li>shows the first 7 characters of the commit&#39;s SHA</li><li>shows the commit&#39;s message</li></ul><h2 id="git-log-stat" tabindex="-1"><a class="header-anchor" href="#git-log-stat" aria-hidden="true">#</a> git log --stat</h2><ul><li>displays the file(s) that have been modified</li><li>displays the number of lines that have been added/removed</li><li>displays a summary line with the total number of modified files and lines that have been added/removed</li></ul><p>PREVIOUSNEXT <img src="https://testksj.oss-cn-beijing.aliyuncs.com/uPic/CRzpxt.png" alt="CRzpxt" loading="lazy"></p><h2 id="git-log-p" tabindex="-1"><a class="header-anchor" href="#git-log-p" aria-hidden="true">#</a> git log -p</h2><ul><li>displays the files that have been modified</li><li>displays the location of the lines that have been added/removed</li><li>displays the actual changes that have been made</li></ul><p>The <code>git log</code> command has a flag that can be used to display the actual changes made to a file. The flag is <code>--patch</code> which can be shortened to just <code>-p</code>:</p><h3 id="annotated-git-log-p-output" tabindex="-1"><a class="header-anchor" href="#annotated-git-log-p-output" aria-hidden="true">#</a> Annotated <code>git log -p</code> Output</h3><p>Using the image above, let&#39;s do a quick recap of the <code>git log -p</code> output: <img src="https://testksj.oss-cn-beijing.aliyuncs.com/uPic/po7H0E.png" alt="po7H0E" loading="lazy"></p><ul><li>🔵 - the file that is being displayed</li><li>🔶 - the hash of the first version of the file and the hash of the second version of the file <ul><li>not usually important, so it&#39;s safe to ignore</li></ul></li><li>❤️ - the old version and current version of the file</li><li>🔍 - the lines where the file is added and how many lines there are <ul><li><code>-15,83</code> indicates that the old version (represented by the <code>-</code>) started at line 15 and that the file had 83 lines</li><li><code>+15,85</code> indicates that the current version (represented by the <code>+</code>) starts at line 15 and that there are now 85 lines...these 85 lines are shown in the patch below</li></ul></li><li>✏️ - the actual changes made in the commit <ul><li>lines that are red and start with a minus (<code>-</code>) were in the original version of the file but have been removed by the commit</li><li>lines that are green and start with a plus (<code>+</code>) are new lines that have been added in the commit</li></ul></li></ul><h3 id="further-research-1" tabindex="-1"><a class="header-anchor" href="#further-research-1" aria-hidden="true">#</a> Further Research</h3>',13),D={href:"https://git-scm.com/docs/git-diff#_generating_patches_with_p",target:"_blank",rel:"noopener noreferrer"},S=o(`<h2 id="git-log-p-stat" tabindex="-1"><a class="header-anchor" href="#git-log-p-stat" aria-hidden="true">#</a> git log -p --stat</h2><p>you can combine flags! <code>git log -p --stat</code>will display the stats info above the patch info. Actually, the order doesn&#39;t matter; <code>git log --stat -p</code> will also show the stats info above the patch info.</p><h2 id="git-log-p-w" tabindex="-1"><a class="header-anchor" href="#git-log-p-w" aria-hidden="true">#</a> git log -p -w</h2><p>show the patch information, but will not highlight lines where _only_whitespace changes have occurred.</p><p>-w --ignore-all-space Ignore whitespace when comparing lines. This ignores differences even if one line has whitespace where the other line has none.</p><p>--ignore-blank-lines Ignore changes whose lines are all blank.</p><h2 id="git-log-p-sha-git-show-sha" tabindex="-1"><a class="header-anchor" href="#git-log-p-sha-git-show-sha" aria-hidden="true">#</a> git log -p SHA / git show SHA</h2><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git log -p fdf5493
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>By supplying a SHA, the <code>git log -p</code> command will <em>start at that commit</em>! No need to scroll through everything! Keep in mind that it will <em>also</em> show all of the commits that were made <em>prior</em> to the supplied SHA.</p><p><code>$ git show fdf5493</code></p><p>The <code>git show</code> command will show <em>only one commit</em>. So don&#39;t get alarmed when you can&#39;t find any other commits - it only shows one. The output of the <code>git show</code> command is exactly the same as the <code>git log -p</code> command. So by default, <code>git show</code> displays:</p><ul><li>the commit</li><li>the author</li><li>the date</li><li>the commit message</li><li>the patch information</li></ul><p>However, <code>git show</code> can be combined with most of the other flags we&#39;ve looked at:</p><ul><li><code>--stat</code> - to show the how many files were changed and the number of lines that were added/removed</li><li><code>-p</code> or <code>--patch</code> - this the default, but if <code>--stat</code> is used, the patch won&#39;t display, so pass <code>-p</code> to add it again</li><li><code>-w</code> - to ignore changes to whitespace</li></ul><h2 id="git-log-oneline-graph-all" tabindex="-1"><a class="header-anchor" href="#git-log-oneline-graph-all" aria-hidden="true">#</a> git log --oneline --graph --all</h2><p>The <code>--graph</code> flag adds the bullets and lines to the leftmost part of the output. This shows the actual <em>branching</em> that&#39;s happening. The <code>--all</code> flag is what displays <em>all</em> of the branches in the repository.</p><h2 id="git-add" tabindex="-1"><a class="header-anchor" href="#git-add" aria-hidden="true">#</a> git add</h2><p>The <code>git add</code> command is used to move files from the Working Directory to the Staging Index.</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git add &lt;file1&gt; &lt;file2&gt; … &lt;fileN&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This command:</p><ul><li>takes a space-separated list of file names</li><li>alternatively, the period <code>.</code> can be used in place of a list of files to tell Git to add the current directory (and all nested files)</li></ul><blockquote><p>TIP: Did you also notice the helpful text that&#39;s located just beneath &quot;Changes to be committed&quot;? It says <code>(use &quot;git rm --cached &lt;file&gt;...&quot; to unstage)</code> This is a hint of what you should do if you accidentally ran <code>git add</code> and gave it the wrong file.</p></blockquote><blockquote><p>As a side note, <code>git rm --cached</code> is not like the shell&#39;s <code>rm</code> command. <code>git rm --cached</code> will not destroy any of your work; it just removes it from the Staging Index.</p></blockquote><blockquote><p>Also, this used the word &quot;unstage&quot;. The act of moving a file from the Working Directory to the Staging Index is called &quot;staging&quot;. If a file has been moved, then it has been &quot;staged&quot;. Moving a file from the Staging Index _back_to the Working Directory will unstage the file. If you read documentation that says &quot;stage the following files&quot; that means you should use the <code>git add</code> command.</p></blockquote><h3 id="stage-remaining-files" tabindex="-1"><a class="header-anchor" href="#stage-remaining-files" aria-hidden="true">#</a> Stage Remaining Files</h3><p>The <code>index.html</code> file has been staged. Let&#39;s stage the other two files. Now we <em>could</em> run the following:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git add css/app.css js/app.js
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>...but that&#39;s a lot of extra typing. We could use a special command line character to help:</p><h3 id="the-period" tabindex="-1"><a class="header-anchor" href="#the-period" aria-hidden="true">#</a> The Period <code>.</code></h3><p>The period refers to the current directory and can be used as a shortcut to refer to all files and directories (including all nested files and directories!).</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git add css/app.css js/app.js
# would become
$ git add .
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The only thing to be careful of is that you might accidentally include more files than you meant to. Right now we <em>want</em> both <code>css/app.css</code> and <code>js/app.js</code> to be staged, so running this command is fine right now. But let&#39;s say you added some images to an <code>img</code> directory but didn&#39;t want to stage them just yet. Running <code>git add .</code> <em>will</em> stage them. If you do stage files that you didn&#39;t mean to, remember that <code>git status</code> will tell you the command to use to &quot;unstage&quot; files.</p><h2 id="git-commit" tabindex="-1"><a class="header-anchor" href="#git-commit" aria-hidden="true">#</a> git commit</h2><p>The <code>git commit</code> command takes files from the Staging Index and saves them in the repository.</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git commit
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This command:</p><ul><li>will open the code editor that is specified in your configuration <ul><li>(check out the Git configuration step from the first lesson to configure your editor)</li></ul></li></ul><p>Inside the code editor:</p><ul><li>a commit message must be supplied</li><li>lines that start with a <code>#</code> are comments and will not be recorded</li><li>save the file after adding a commit message</li><li>close the editor to make the commit</li></ul><p>Then, use <code>git log</code> to review the commit you just made!</p><h3 id="further-research-2" tabindex="-1"><a class="header-anchor" href="#further-research-2" aria-hidden="true">#</a> Further Research</h3>`,41),$={href:"https://help.github.com/articles/associating-text-editors-with-git/",target:"_blank",rel:"noopener noreferrer"},F={href:"https://git-scm.com/book/en/v2/Getting-Started-First-Time-Git-Setup",target:"_blank",rel:"noopener noreferrer"},M=e("h3",{id:"bypass-the-editor-with-the-m-flag",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#bypass-the-editor-with-the-m-flag","aria-hidden":"true"},"#"),t(" Bypass The Editor With The "),e("code",null,"-m"),t(" Flag")],-1),z=e("blockquote",null,[e("p",null,[t("TIP: If the commit message you're writing is short and you don't want to wait for your code editor to open up to type it out, you can pass your message directly on the command line with the "),e("code",null,"-m"),t(" flag: "),e("code",null,'$ git commit -m "Initial commit"')])],-1),N=e("h3",{id:"good-commit-message",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#good-commit-message","aria-hidden":"true"},"#"),t(" Good commit message")],-1),P={href:"https://chris.beams.io/posts/git-commit/",target:"_blank",rel:"noopener noreferrer"},L={href:"https://medium.com/@preslavrachev/what-s-with-the-50-72-rule-8a906f61f09c#.jwprsco0n",target:"_blank",rel:"noopener noreferrer"},O={href:"http://tbaggery.com/2008/04/19/a-note-about-git-commit-messages.html",target:"_blank",rel:"noopener noreferrer"},U=o(`<p><strong>Do</strong></p><ul><li>do keep the message short (less than 60-ish characters)</li><li>do explain <em>what</em> the commit does (not <em>how</em> or <em>why</em>!)</li></ul><p><strong>Do not</strong></p><ul><li>do not explain <em>why</em> the changes are made (more on this below)</li><li>do not explain <em>how</em> the changes are made (that&#39;s what <code>git log -p</code> is for!)</li><li>do not use the word &quot;and&quot; <ul><li>if you have to use &quot;and&quot;, your commit message is probably doing too many changes - break the changes into separate commits</li><li>e.g. &quot;make the background color pink <em>and</em> increase the size of the sidebar&quot;</li></ul></li></ul><p>The best way that I&#39;ve found to come up with a commit message is to finish this phrase, &quot;This commit will...&quot;. However, you finish that phrase, use <em>that</em> as your commit message.</p><p>Above all, <em><strong>be consistent</strong></em> in how you write your commit messages!</p><h3 id="explain-the-why" tabindex="-1"><a class="header-anchor" href="#explain-the-why" aria-hidden="true">#</a> Explain the Why</h3><p>If you need to explain <em>why</em> a commit needs to be made, you can!</p><p>When you&#39;re writing the commit message, the first line is the message itself. After the message, leave a blank line, and then type out the body or explanation including details about why the commit is needed (e.g. URL links).</p><p>Here&#39;s what a commit message edit screen might look like: <img src="https://testksj.oss-cn-beijing.aliyuncs.com/uPic/pID4IX.png" alt="pID4IX" loading="lazy"></p><h2 id="git-diff" tabindex="-1"><a class="header-anchor" href="#git-diff" aria-hidden="true">#</a> git diff</h2><p>To recap, the <code>git diff</code> command is used to see changes that have been made but haven&#39;t been committed, yet:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git diff
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This command displays:</p><ul><li>the files that have been modified</li><li>the location of the lines that have been added/removed</li><li>the actual changes that have been made</li></ul><h3 id="further-research-3" tabindex="-1"><a class="header-anchor" href="#further-research-3" aria-hidden="true">#</a> Further Research</h3>`,16),W={href:"https://git-scm.com/docs/git-diff",target:"_blank",rel:"noopener noreferrer"},Q=e("h2",{id:"git-ignore",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#git-ignore","aria-hidden":"true"},"#"),t(" git ignore")],-1),Y=e("p",null,[t("If you want to keep a file in your project's directory structure but make sure it isn't accidentally committed to the project, you can use the specially named file, "),e("code",null,".gitignore"),t(" (note the dot at the front, it's important!). Add this file to your project in the same directory that the hidden "),e("code",null,".git"),t(" directory is located. All you have to do is list the "),e("em",null,"names"),t(" of files that you want Git to ignore (not track) and it will ignore them.")],-1),V=e("h3",{id:"further-research-4",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#further-research-4","aria-hidden":"true"},"#"),t(" Further Research")],-1),X={href:"https://git-scm.com/book/en/v2/Git-Basics-Recording-Changes-to-the-Repository#Ignoring-Files",target:"_blank",rel:"noopener noreferrer"},J={href:"https://git-scm.com/docs/gitignore#_pattern_format",target:"_blank",rel:"noopener noreferrer"},Z={href:"https://help.github.com/articles/ignoring-files/",target:"_blank",rel:"noopener noreferrer"},K={href:"https://www.gitignore.io/",target:"_blank",rel:"noopener noreferrer"},ee=e("h2",{id:"globbing-crash-course",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#globbing-crash-course","aria-hidden":"true"},"#"),t(" Globbing Crash Course")],-1),te=e("code",null,".gitignore",-1),ie={href:"https://en.wikipedia.org/wiki/Glob_(programming)",target:"_blank",rel:"noopener noreferrer"},ae=o(`<p>Globbing lets you use special characters to match patterns/characters. In the <code>.gitignore</code> file, you can use the following:</p><ul><li>blank lines can be used for spacing</li><li><code>#</code> - marks line as a comment</li><li><code>*</code> - matches 0 or more characters</li><li><code>?</code> - matches 1 character</li><li><code>[abc]</code> - matches a, b, <em>or</em> c</li><li><code>**</code> - matches nested directories - <code>a/**/z</code> matches <ul><li>a/z</li><li>a/b/z</li><li>a/b/c/z</li></ul></li></ul><p>So if all of the 50 images are JPEG images in the &quot;samples&quot; folder, we could add the following line to <code>.gitignore</code> to have Git ignore all 50 images.</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>samples/*.jpg
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="git-tag" tabindex="-1"><a class="header-anchor" href="#git-tag" aria-hidden="true">#</a> git tag</h2><h3 id="tag-the-most-recent-commit" tabindex="-1"><a class="header-anchor" href="#tag-the-most-recent-commit" aria-hidden="true">#</a> tag the most recent commit</h3><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git tag -a v1.0
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><blockquote><p>CAREFUL: In the command above (<code>git tag -a v1.0</code>) the <code>-a</code> flag is used. This flag tells Git to create an <em>annotated</em> flag. If you don&#39;t provide the flag (i.e. <code>git tag v1.0</code>) then it&#39;ll create what&#39;s called a <em>lightweight</em> tag.</p></blockquote><blockquote><p>Annotated tags are recommended because they include a lot of extra information such as:</p><ul><li>the person who made the tag</li><li>the date the tag was made</li><li>a message for the tag</li></ul></blockquote><h3 id="adding-a-tag-to-a-past-commit" tabindex="-1"><a class="header-anchor" href="#adding-a-tag-to-a-past-commit" aria-hidden="true">#</a> Adding A Tag To A Past Commit</h3><p>Running <code>git tag -a v1.0</code> will . But what if you wanted to tag a commit that occurred farther back in the repo&#39;s history?</p><p>All you have to do is provide the SHA of the commit you want to tag!</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git tag -a v1.0 a87984
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="further-research-5" tabindex="-1"><a class="header-anchor" href="#further-research-5" aria-hidden="true">#</a> Further Research</h3>`,14),oe={href:"https://git-scm.com/book/en/v2/Git-Basics-Tagging",target:"_blank",rel:"noopener noreferrer"},ne={href:"https://git-scm.com/docs/git-tag",target:"_blank",rel:"noopener noreferrer"},re=o(`<h2 id="git-tag-d" tabindex="-1"><a class="header-anchor" href="#git-tag-d" aria-hidden="true">#</a> git tag -d</h2><p>A Git tag can be deleted with the <code>-d</code> flag (for <em>delete</em>!) and the name of the tag:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git tag -d v1.0
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><figure><img src="https://testksj.oss-cn-beijing.aliyuncs.com/uPic/QwBmPt.png" alt="QwBmPt" tabindex="0" loading="lazy"><figcaption>QwBmPt</figcaption></figure><h2 id="git-branch" tabindex="-1"><a class="header-anchor" href="#git-branch" aria-hidden="true">#</a> git branch</h2><p>It can be used to:</p><ul><li>list all branch names in the repository</li><li>create new branches <code>git branch sidebar</code></li><li>delete branches</li></ul><p>If we type out just <code>git branch</code> it will list out the branches in a repository</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>git branch alt-sidebar-loc 42a69f
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>It creates a new branch called <code>alt-sidebar-loc</code> and has it pointing at the commit with the SHA <code>42a69f</code></p><p>Deleting something can be quite nerve-wracking. Don&#39;t worry, though. Git won&#39;t let you delete a branch if it has commits on it that aren&#39;t on any other branch (meaning the commits are unique to the branch that&#39;s about to be deleted). If you created the <code>sidebar</code> branch, added commits to it, and then tried to delete it with the <code>git branch -d sidebar</code>, Git wouldn&#39;t let you delete the branch because you can&#39;t delete a branch that you&#39;re currently on. If you switched to the <code>master</code> branch and tried to delete the <code>sidebar</code> branch, Git <em>also</em> wouldn&#39;t let you do that because those new commits on the <code>sidebar</code> branch would be lost! To force deletion, you need to use a capital D flag - <code>git branch -D sidebar</code>.</p><h3 id="change-branch-name" tabindex="-1"><a class="header-anchor" href="#change-branch-name" aria-hidden="true">#</a> change branch name</h3><p>\`$ git branch-m &amp;NEWBRANCHNAME&gt;</p><h2 id="git-branch-recap" tabindex="-1"><a class="header-anchor" href="#git-branch-recap" aria-hidden="true">#</a> Git Branch Recap</h2><p>To recap, the <code>git branch</code> command is used to manage branches in Git:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code># to list all branches
$ git branch

# to create a new &quot;footer-fix&quot; branch
$ git branch footer-fix

# to delete the &quot;footer-fix&quot; branch
$ git branch -d footer-fix
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>This command is used to:</p><ul><li>list out local branches</li><li>create new branches</li><li>remove branches</li></ul><h3 id="further-research-6" tabindex="-1"><a class="header-anchor" href="#further-research-6" aria-hidden="true">#</a> Further Research</h3>`,19),se={href:"https://git-scm.com/book/en/v2/Git-Branching-Basic-Branching-and-Merging",target:"_blank",rel:"noopener noreferrer"},le={href:"http://learngitbranching.js.org/",target:"_blank",rel:"noopener noreferrer"},de={href:"https://www.atlassian.com/git/tutorials/using-branches",target:"_blank",rel:"noopener noreferrer"},ce=o(`<p>PREVIOUSNEXT</p><h2 id="git-checkout" tabindex="-1"><a class="header-anchor" href="#git-checkout" aria-hidden="true">#</a> git checkout</h2><p>It&#39;s important to understand how this command works. Running this command will:</p><ul><li>remove all files and directories from the Working Directory that Git is tracking <ul><li>(files that Git tracks are stored in the repository, so nothing is lost)</li></ul></li><li>go into the repository and pull out all of the files and directories of the commit that the branch points to</li></ul><p>So this will remove all of the files that are referenced by commits in the master branch. It will replace them with the files that are referenced by the commits in the sidebar branch.</p><h3 id="git-checkout-b" tabindex="-1"><a class="header-anchor" href="#git-checkout-b" aria-hidden="true">#</a> git checkout -b</h3><p><code>git checkout</code> command can actually create a new branch, too? If you provide the <code>-b</code> flag, you can create a branch <em>and</em> switch to it all in one command.</p><h2 id="merging" tabindex="-1"><a class="header-anchor" href="#merging" aria-hidden="true">#</a> merging</h2><p>Combining branches together is called <strong>merging</strong>. Pay attention to the two main types of merges in Git, a regular <strong>merge</strong> and a <strong>Fast-forward merge</strong>. Remember that making a merge makes a commit.</p><h2 id="git-merge" tabindex="-1"><a class="header-anchor" href="#git-merge" aria-hidden="true">#</a> git merge</h2><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git merge &lt;name-of-branch-to-merge-in&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>When a merge happens, Git will:</p><ul><li>look at the branches that it&#39;s going to merge</li><li>look back along the branch&#39;s history to find a single commit that <em>both</em> branches have in their commit history</li><li>combine the lines of code that were changed on the separate branches together</li><li>makes a commit to record the merge</li><li>When we merge, we&#39;re merging some other branch into the current (checked-out) branch. We&#39;re not merging two branches into a new branch. We&#39;re not merging the current branch into the other branch.</li></ul><h3 id="fast-forward-merge" tabindex="-1"><a class="header-anchor" href="#fast-forward-merge" aria-hidden="true">#</a> Fast-forward Merge</h3><p>Now, since <code>footer</code> is directly ahead of <code>master</code>, this merge is one of the easiest merges to do. Merging <code>footer</code> into <code>master</code> will cause a <strong>Fast-forward merge</strong>. A Fast-forward merge will just move the currently checked out branch <em>forward</em> until it points to the same commit that the other branch (in this case, <code>footer</code>) is pointing to.</p><p>To merge in the <code>footer</code> branch, run command in <code>master</code> branch:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git merge footer
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><figure><img src="https://testksj.oss-cn-beijing.aliyuncs.com/uPic/T1DGyH.png" alt="T1DGyH" tabindex="0" loading="lazy"><figcaption>T1DGyH</figcaption></figure><p>After merge <img src="https://testksj.oss-cn-beijing.aliyuncs.com/uPic/wlryRx.png" alt="wlryRx" loading="lazy"></p><h3 id="regular-merge" tabindex="-1"><a class="header-anchor" href="#regular-merge" aria-hidden="true">#</a> Regular merge</h3><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git merge sidebar
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Because this combines two divergent branches, a commit is going to be made. Since this is a <em>merge commit</em> a default message is already supplied. You can change the message if you want, but it&#39;s common practice to use the default merge commit message. So when your code editor opens with the message, just close it again and accept that commit message. <img src="https://testksj.oss-cn-beijing.aliyuncs.com/uPic/cQGACU.png" alt="cQGACU" loading="lazy"><img src="https://testksj.oss-cn-beijing.aliyuncs.com/uPic/Fo29vZ.png" alt="Fo29vZ" loading="lazy"></p><h3 id="merge-recap" tabindex="-1"><a class="header-anchor" href="#merge-recap" aria-hidden="true">#</a> Merge Recap</h3><p>To recap, the <code>git merge</code> command is used to combine branches in Git:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git merge &lt;other-branch&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>There are two types of merges:</p><ul><li>Fast-forward merge – the branch being merged in must be <em>ahead</em> of the checked out branch. The checked out branch&#39;s pointer will just be moved forward to point to the same commit as the other branch.</li><li>the regular type of merge <ul><li>two divergent branches are combined</li><li>a merge commit is created</li></ul></li></ul><h3 id="further-research-7" tabindex="-1"><a class="header-anchor" href="#further-research-7" aria-hidden="true">#</a> Further Research</h3>`,28),he={href:"https://git-scm.com/book/en/v2/Git-Branching-Basic-Branching-and-Merging#Basic-Merging",target:"_blank",rel:"noopener noreferrer"},ge={href:"https://git-scm.com/docs/git-merge",target:"_blank",rel:"noopener noreferrer"},me={href:"https://www.atlassian.com/git/tutorials/git-merge",target:"_blank",rel:"noopener noreferrer"},ue=o(`<h2 id="merge-conflicts" tabindex="-1"><a class="header-anchor" href="#merge-conflicts" aria-hidden="true">#</a> merge conflicts</h2><h3 id="merge-conflict-output-explained" tabindex="-1"><a class="header-anchor" href="#merge-conflict-output-explained" aria-hidden="true">#</a> Merge Conflict Output Explained</h3><p>The output that shows in the Terminal is:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git merge heading-update 
Auto-merging index.html
CONFLICT (content): Merge conflict in index.html
Automatic merge failed; fix conflicts and then commit the result.
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="merge-conflict-indicators-explanation" tabindex="-1"><a class="header-anchor" href="#merge-conflict-indicators-explanation" aria-hidden="true">#</a> Merge Conflict Indicators Explanation</h3><figure><img src="https://testksj.oss-cn-beijing.aliyuncs.com/uPic/8aGJEi.png" alt="8aGJEi" tabindex="0" loading="lazy"><figcaption>8aGJEi</figcaption></figure><p>The editor has the following merge conflict indicators:</p><ul><li><code>&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD</code> everything below this line (until the next indicator) shows you what&#39;s on the current branch</li><li><code>||||||| merged common ancestors</code> everything below this line (until the next indicator) shows you what the original lines were</li><li><code>=======</code> is the end of the original lines, everything that follows (until the next indicator) is what&#39;s on the branch that&#39;s being merged in</li><li><code>&gt;&gt;&gt;&gt;&gt;&gt;&gt; heading-update</code> is the ending indicator of what&#39;s on the branch that&#39;s being merged in (in this case, the <code>heading-update</code> branch)</li></ul><h3 id="resolving-a-merge-conflict" tabindex="-1"><a class="header-anchor" href="#resolving-a-merge-conflict" aria-hidden="true">#</a> Resolving A Merge Conflict</h3><p>Git is using the merge conflict indicators to show you what lines caused the merge conflict on the two different branches as well as what the original line used to have. So to resolve a merge conflict, you need to:</p><ol><li>choose which line(s) to keep</li><li>remove all lines with indicators</li></ol><figure><img src="https://video.udacity-data.com/topher/2017/February/58a74ced_ud123-l5-resolve-merge-conflict/ud123-l5-resolve-merge-conflict.gif" alt="Code editor where showing the merge conflict indicators being removed and the page heading being updated." tabindex="0" loading="lazy"><figcaption>Code editor where showing the merge conflict indicators being removed and the page heading being updated.</figcaption></figure><p>Once you&#39;ve removed all lines with merge conflict indicators and have selected what heading you want to use, just save the file, add it to the staging index, and commit it!</p><h3 id="merge-conflict-recap" tabindex="-1"><a class="header-anchor" href="#merge-conflict-recap" aria-hidden="true">#</a> Merge Conflict Recap</h3><p>A merge conflict happens when the same line or lines have been changed on different branches that are being merged. Git will pause mid-merge telling you that there is a conflict and will tell you in what file or files the conflict occurred. To resolve the conflict in a file:</p><ul><li>locate and remove all lines with merge conflict indicators</li><li>determine what to keep</li><li>save the file(s)</li><li>stage the file(s)</li><li>make a commit</li></ul><p>Be careful that a file might have merge conflicts in multiple parts of the file, so make sure you check the entire file for merge conflict indicators - a quick search for <code>&lt;&lt;&lt;</code> should help you locate all of them.</p><h3 id="further-research-8" tabindex="-1"><a class="header-anchor" href="#further-research-8" aria-hidden="true">#</a> Further Research</h3>`,18),pe={href:"https://git-scm.com/book/en/v2/Git-Branching-Basic-Branching-and-Merging#Basic-Merge-Conflicts",target:"_blank",rel:"noopener noreferrer"},fe={href:"https://git-scm.com/docs/git-merge#_how_conflicts_are_presented",target:"_blank",rel:"noopener noreferrer"},be=o('<h1 id="undo-changes" tabindex="-1"><a class="header-anchor" href="#undo-changes" aria-hidden="true">#</a> Undo changes</h1><h2 id="changing-the-last-commit-git-commit-amend" tabindex="-1"><a class="header-anchor" href="#changing-the-last-commit-git-commit-amend" aria-hidden="true">#</a> Changing The Last Commit git commit --amend</h2><p>If your Working Directory is clean (<strong>meaning there aren&#39;t any uncommitted changes in the repository</strong>), then running <code>git commit --amend</code> will let you provide a new commit message. Your code editor will open up and display the original commit message. Just fix a misspelling or completely reword it! Then save it and close the editor to lock in the new commit message.</p><h3 id="add-forgotten-files-to-commit" tabindex="-1"><a class="header-anchor" href="#add-forgotten-files-to-commit" aria-hidden="true">#</a> Add Forgotten Files To Commit</h3><p>To do get the forgotten link included, just:</p><ul><li>edit the file(s)</li><li>save the file(s)</li><li>stage the file(s)</li><li>and run <code>git commit --amend</code></li></ul><h2 id="reverse-previous-commit-git-revert-sha" tabindex="-1"><a class="header-anchor" href="#reverse-previous-commit-git-revert-sha" aria-hidden="true">#</a> Reverse previous commit git revert SHA</h2><p>To recap, the <code>git revert</code> command is used to reverse a previously made commit:</p><p><code>$ git revert SHA-of-commit-to-revert</code></p><p>This command:</p><ul><li>will undo the changes that were made by the provided commit</li><li>creates a new commit to record the change <img src="https://testksj.oss-cn-beijing.aliyuncs.com/uPic/tDT6ft.png" alt="tDT6ft" loading="lazy"></li></ul><h3 id="further-research-9" tabindex="-1"><a class="header-anchor" href="#further-research-9" aria-hidden="true">#</a> Further Research</h3>',12),ve={href:"https://git-scm.com/docs/git-revert",target:"_blank",rel:"noopener noreferrer"},we={href:"https://www.atlassian.com/git/tutorials/undoing-changes",target:"_blank",rel:"noopener noreferrer"},ye=o('<h2 id="reset-erase-commit" tabindex="-1"><a class="header-anchor" href="#reset-erase-commit" aria-hidden="true">#</a> Reset(erase) commit</h2><blockquote><h2 id="⚠️-resetting-is-dangerous-⚠️" tabindex="-1"><a class="header-anchor" href="#⚠️-resetting-is-dangerous-⚠️" aria-hidden="true">#</a> ⚠️ Resetting Is Dangerous ⚠️</h2></blockquote><blockquote><p>You&#39;ve got to be careful with Git&#39;s resetting capabilities. This is one of the few commands that lets you erase commits from the repository. If a commit is no longer in the repository, then its content is gone.</p></blockquote><blockquote><p>To alleviate the stress a bit, Git <em>does</em> keep track of everything for about 30 days before it completely erases anything. To access this content, you&#39;ll need to use the <code>git reflog</code> command. Check out these links for more info:</p></blockquote>',4),ke={href:"https://git-scm.com/docs/git-reflog",target:"_blank",rel:"noopener noreferrer"},xe={href:"https://www.atlassian.com/git/tutorials/rewriting-history",target:"_blank",rel:"noopener noreferrer"},_e={href:"http://gitready.com/intermediate/2009/02/09/reflog-your-safety-net.html",target:"_blank",rel:"noopener noreferrer"},Te=o(`<h3 id="relative-commit-references" tabindex="-1"><a class="header-anchor" href="#relative-commit-references" aria-hidden="true">#</a> Relative Commit References</h3><p>Here&#39;s how we can refer to previous commits:</p><ul><li><p><code>^</code> – indicates the parent commit</p></li><li><p><code>~</code> – indicates the <em>first</em> parent commit</p></li><li><p>the parent commit – the following indicate the parent commit of the current commit</p><ul><li>HEAD^</li><li>HEAD~</li><li>HEAD~1</li></ul></li><li><p>the grandparent commit – the following indicate the grandparent commit of the current commit</p><ul><li>HEAD^^</li><li>HEAD~2</li></ul></li><li><p>the great-grandparent commit – the following indicate the great-grandparent commit of the current commit</p><ul><li>HEAD^^^</li><li>HEAD~3</li></ul></li></ul><p>The main difference between the <code>^</code> and the <code>~</code> is when a commit is created <em>from a merge</em>. A merge commit has <em>two</em> parents. With a merge commit, the <code>^</code> reference is used to indicate the <em>first</em> parent of the commit while <code>^2</code> indicates the <em>second</em> parent. The first parent is the branch you were on when you ran <code>git merge</code> while the second parent is the branch that was merged in.</p><figure><img src="https://testksj.oss-cn-beijing.aliyuncs.com/uPic/4pUk4r.png" alt="4pUk4r" tabindex="0" loading="lazy"><figcaption>4pUk4r</figcaption></figure><p>which commit is referenced by <code>HEAD~4^2</code>? ---f69811c Which commit is referenced by HEAD~6? ---209752a</p><h2 id="git-reset" tabindex="-1"><a class="header-anchor" href="#git-reset" aria-hidden="true">#</a> git reset</h2><p>The <code>git reset</code> command is used to reset (erase) commits:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git reset &lt;reference-to-commit&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>It can be used to:</p><ul><li>move the HEAD and current branch pointer to the referenced commit</li><li>erase commits</li><li>move committed changes to the staging index</li><li>unstage committed changes</li></ul><h3 id="git-reset-s-flags" tabindex="-1"><a class="header-anchor" href="#git-reset-s-flags" aria-hidden="true">#</a> Git Reset&#39;s Flags</h3><p>The way that Git determines if it erases, stages previously committed changes, or unstages previously committed changes is by the flag that&#39;s used. The flags are:</p><ul><li><code>--mixed</code></li><li><code>--soft</code></li><li><code>--hard</code></li></ul><p><img src="https://testksj.oss-cn-beijing.aliyuncs.com/uPic/OfXQE4.png" alt="OfXQE4" loading="lazy"><img src="https://testksj.oss-cn-beijing.aliyuncs.com/uPic/Hs0Ox0.png" alt="Hs0Ox0" loading="lazy"></p><blockquote><h2 id="💡-backup-branch-💡" tabindex="-1"><a class="header-anchor" href="#💡-backup-branch-💡" aria-hidden="true">#</a> 💡 Backup Branch 💡</h2></blockquote><blockquote><p>Remember that using the <code>git reset</code> command will <em>erase</em> commits from the current branch. So if you want to follow along with all the resetting stuff that&#39;s coming up, you&#39;ll need to create a branch on the current commit that you can use as a backup.</p></blockquote><blockquote><p>Before I do any resetting, I usually create a <code>backup</code> branch on the most-recent commit so that I can get back to the commits if I make a mistake:</p></blockquote><blockquote><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git branch backup
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></blockquote><h3 id="reset-s-mixed-flag" tabindex="-1"><a class="header-anchor" href="#reset-s-mixed-flag" aria-hidden="true">#</a> Reset&#39;s <code>--mixed</code> Flag</h3><p>Let&#39;s look at each one of these flags.</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>* 9ec05ca (HEAD -&gt; master) Revert &quot;Set page heading to &quot;Quests &amp; Crusades&quot;&quot;
* db7e87a Set page heading to &quot;Quests &amp; Crusades&quot;
* 796ddb0 Merge branch &#39;heading-update&#39;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Using the sample repo above with <code>HEAD</code> pointing to <code>master</code> on commit <code>9ec05ca</code>, running <code>git reset --mixed HEAD^</code> will take the changes made in commit <code>9ec05ca</code> and move them to the working directory.</p><figure><img src="https://video.udacity-data.com/topher/2017/February/58a4e242_ud123-l6-git-revert-mixed/ud123-l6-git-revert-mixed.png" alt="The Terminal application showing the result of resetting with the  flag. The changes are unstaged." tabindex="0" loading="lazy"><figcaption>The Terminal application showing the result of resetting with the <code>--mixed</code> flag. The changes are unstaged.</figcaption></figure><p><em>The Terminal application showing the result of resetting with the <code>--mixed</code> flag. The changes are unstaged.</em></p><blockquote><h2 id="💡-back-to-normal-💡" tabindex="-1"><a class="header-anchor" href="#💡-back-to-normal-💡" aria-hidden="true">#</a> 💡 Back To Normal 💡</h2></blockquote><blockquote><p>If you created the <code>backup</code> branch prior to resetting anything, then you can easily get back to having the <code>master</code> branch point to the same commit as the <code>backup</code> branch. You&#39;ll just need to:</p><ol><li>remove the uncommitted changes from the working directory</li><li>merge <code>backup</code> into <code>master</code> (which will cause a Fast-forward merge and move <code>master</code> up to the same point as <code>backup</code>)</li></ol></blockquote><blockquote><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ git checkout -- index.html
$ git merge backup
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div></blockquote><h3 id="reset-s-soft-flag" tabindex="-1"><a class="header-anchor" href="#reset-s-soft-flag" aria-hidden="true">#</a> Reset&#39;s <code>--soft</code> Flag</h3><p>Let&#39;s use the same few commits and look at how the <code>--soft</code> flag works:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>* 9ec05ca (HEAD -&gt; master) Revert &quot;Set page heading to &quot;Quests &amp; Crusades&quot;&quot;
* db7e87a Set page heading to &quot;Quests &amp; Crusades&quot;
* 796ddb0 Merge branch &#39;heading-update&#39;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Running <code>git reset --soft HEAD^</code> will take the changes made in commit <code>9ec05ca</code> and move them directly to the Staging Index.</p><figure><img src="https://video.udacity-data.com/topher/2017/February/58a4e29a_ud123-l6-git-revert-soft/ud123-l6-git-revert-soft.png" alt="The Terminal application showing the result of resetting with the  flag. The changes are moved to the Staging Index." tabindex="0" loading="lazy"><figcaption>The Terminal application showing the result of resetting with the <code>--soft</code> flag. The changes are moved to the Staging Index.</figcaption></figure><p><em>The Terminal application showing the result of resetting with the <code>--soft</code> flag. The changes are moved to the Staging Index.</em></p><h3 id="reset-s-hard-flag" tabindex="-1"><a class="header-anchor" href="#reset-s-hard-flag" aria-hidden="true">#</a> Reset&#39;s <code>--hard</code> Flag</h3><p>Last but not least, let&#39;s look at the <code>--hard</code> flag:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>* 9ec05ca (HEAD -&gt; master) Revert &quot;Set page heading to &quot;Quests &amp; Crusades&quot;&quot;
* db7e87a Set page heading to &quot;Quests &amp; Crusades&quot;
* 796ddb0 Merge branch &#39;heading-update&#39;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Running <code>git reset --hard HEAD^</code> will take the changes made in commit <code>9ec05ca</code> and erases them.</p><figure><img src="https://video.udacity-data.com/topher/2017/February/58a4e2c2_ud123-l6-git-revert-hard/ud123-l6-git-revert-hard.png" alt="The Terminal application showing the result of resetting with the  flag. The changes are moved erased." tabindex="0" loading="lazy"><figcaption>The Terminal application showing the result of resetting with the <code>--hard</code> flag. The changes are moved erased.</figcaption></figure><p><em>The Terminal application showing the result of resetting with the <code>--hard</code> flag. The changes are moved erased.</em></p><h3 id="further-research-10" tabindex="-1"><a class="header-anchor" href="#further-research-10" aria-hidden="true">#</a> Further Research</h3>`,41),Ge={href:"https://git-scm.com/docs/git-reset",target:"_blank",rel:"noopener noreferrer"},qe={href:"https://git-scm.com/book/en/v2/Git-Tools-Reset-Demystified",target:"_blank",rel:"noopener noreferrer"},Re={href:"https://git-scm.com/book/en/v2/Git-Tools-Revision-Selection#Ancestry-References",target:"_blank",rel:"noopener noreferrer"},Ae=e("h2",{id:"expand-on-your-git-skills",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#expand-on-your-git-skills","aria-hidden":"true"},"#"),t(" Expand On Your Git Skills")],-1),Ee=e("li",null,"take the companion GitHub course",-1),je={href:"https://dotfiles.github.io/",target:"_blank",rel:"noopener noreferrer"},Ie=e("li",null,"develop the next, awesome feature for your personal project",-1),He={href:"https://github.com/jlord/git-it-electron",target:"_blank",rel:"noopener noreferrer"},Be={href:"https://udacity.github.io/git-styleguide/",target:"_blank",rel:"noopener noreferrer"},Ce=o('<h1 id="remote-control" tabindex="-1"><a class="header-anchor" href="#remote-control" aria-hidden="true">#</a> Remote Control</h1><h2 id="add-remote-connections" tabindex="-1"><a class="header-anchor" href="#add-remote-connections" aria-hidden="true">#</a> Add remote connections</h2><p><code>$ git remote add ＜REMOTENAME＞ ＜URL&gt; </code></p><h2 id="set-a-url-to-a-remote" tabindex="-1"><a class="header-anchor" href="#set-a-url-to-a-remote" aria-hidden="true">#</a> Set a URL to a remote</h2><p><code>$ git remote set-urL &lt;REMOTENAME&gt; &lt;URL&gt;</code></p><h2 id="pull-in-changes" tabindex="-1"><a class="header-anchor" href="#pull-in-changes" aria-hidden="true">#</a> Pull in changes</h2><p><code>$ git pull ＜REMOTENAME&gt; ＜BRANCHNAME&gt;</code></p><h2 id="view-remote-addresses" tabindex="-1"><a class="header-anchor" href="#view-remote-addresses" aria-hidden="true">#</a> View remote addresses</h2><p><code>$ git remote-v</code></p><h2 id="push-changes" tabindex="-1"><a class="header-anchor" href="#push-changes" aria-hidden="true">#</a> Push changes</h2><p><code>git push ＜REMOTENAME&gt; ＜BRANCH&gt;</code></p><h2 id="delete-a-remote-branch" tabindex="-1"><a class="header-anchor" href="#delete-a-remote-branch" aria-hidden="true">#</a> Delete a remote branch</h2><p><code>$ git push &lt;REMOTENAME&gt;--delete &lt;BRANCHNAME&gt;</code></p><h2 id="fork-and-clone" tabindex="-1"><a class="header-anchor" href="#fork-and-clone" aria-hidden="true">#</a> fork and clone</h2><p>To be sure you have the correct remotes set up, type git remote-v to list out the addresses you have stored. You should have an &#39;origin&#39; remote with your fork&#39;s address and then an &#39;upstream&#39; remote with the address to the original, the URL noted above in this step. <code>$ git remote add upstream https://github.com/jlord/patchwork.git</code></p><p>Add a remote <code>$ git remote add ＜REMOTENAME&gt; ＜URL&gt;</code></p><p>Change a remote URL <code>$ git remote set-url ＜REMOTENAME&gt; ＜URL&gt;</code></p><p>View remote connections <code>$ git remote-V</code></p><p>Pullin changes from a remote branch <code>$ git pull &lt;REMOTENAME&gt; &lt;REMOTEBRANCH&gt;</code></p><p>See changes to the remote before you pullin <code>$ git fetch--dry-run</code></p>',20);function De(Se,$e){const i=l("ExternalLinkIcon");return r(),s("div",null,[c,e("ul",null,[e("li",null,[e("p",null,[h,t(" - where all "),g,t(" configuration settings are stored."),m,t(" From the "),e("a",u,[t("Git Book"),a(i)]),t(":")]),p,f]),b]),v,w,e("ul",null,[e("li",null,[e("a",y,[t("Git Internals - Plumbing and Porcelain"),a(i)]),t(" (advanced - bookmark this and check it out later)")]),e("li",null,[e("a",k,[t("Customizing Git - Git Hooks"),a(i)])])]),x,e("ul",null,[e("li",null,[e("a",_,[t("Cloning an Existing Repository"),a(i)])]),e("li",null,[e("a",T,[t("git clone docs"),a(i)])]),e("li",null,[e("a",G,[t("git clone Tutorial"),a(i)])])]),q,e("ul",null,[e("li",null,[e("a",R,[t("Checking the Status of Your Files"),a(i)])]),e("li",null,[e("a",A,[t("git status docs"),a(i)])]),e("li",null,[e("a",E,[t("git status Tutorial"),a(i)])])]),j,e("p",null,[t("If you're not used to a pager on the command line, navigating in "),e("a",I,[t("Less"),a(i)]),t(" can be a bit odd. Here are some helpful keys:")]),H,e("p",null,[t("Flags can be used to alter how a program functions and/or what is displayed. To learn more about command line programs and flags, check out our course "),e("a",B,[t("Linux Command Line Basics"),a(i)]),t(".")]),C,e("ul",null,[e("li",null,[e("a",D,[t("Generating patches with -p"),a(i)]),t(" from the Git docs")])]),S,e("ul",null,[e("li",null,[e("a",$,[t("Associating text editors with Git"),a(i)]),t(" from GitHub Help Docs")]),e("li",null,[e("a",F,[t("Getting Started - First-Time Git Setup"),a(i)]),t(" from Git book")])]),M,z,N,e("p",null,[t(`Now, what makes a "good" commit message? That's a great question and has been `),e("a",P,[t("written about"),a(i)]),t(" "),e("a",L,[t("a number"),a(i)]),t(" "),e("a",O,[t("of times"),a(i)]),t(". Here are some important things to think about when crafting a good commit message:")]),U,e("ul",null,[e("li",null,[e("a",W,[t("git diff"),a(i)]),t(" from the Git Docs")])]),Q,Y,V,e("ul",null,[e("li",null,[e("a",X,[t("Ignoring files"),a(i)]),t(" from the Git Book")]),e("li",null,[e("a",J,[t("gitignore"),a(i)]),t(" from the Git Docs")]),e("li",null,[e("a",Z,[t("Ignoring files"),a(i)]),t(" from the GitHub Docs")]),e("li",null,[e("a",K,[t("gitignore.io"),a(i)])])]),ee,e("p",null,[t("Let's say that you add 50 images to your project, but want Git to ignore all of them. Does this mean you have to list each and every filename in the "),te,t(" file? Oh gosh no, that would be crazy! Instead, you can use a concept called [globbing]("),e("a",ie,[t("https://en.wikipedia.org/wiki/Glob_(programming)"),a(i)]),t(".")]),ae,e("ul",null,[e("li",null,[e("a",oe,[t("Git Basics - Tagging"),a(i)]),t(" from the Git Book")]),e("li",null,[e("a",ne,[t("Git Tag"),a(i)]),t(" from the Git Docs")])]),re,e("ul",null,[e("li",null,[e("a",se,[t("Git Branching - Basic Branching and Merging"),a(i)]),t(" from the Git Docs")]),e("li",null,[e("a",le,[t("Learn Git Branching"),a(i)])]),e("li",null,[e("a",de,[t("Git Branching Tutorial"),a(i)]),t(" from the Atlassian Blog")])]),ce,e("ul",null,[e("li",null,[e("a",he,[t("Basic Merging"),a(i)]),t(" from Git Book")]),e("li",null,[e("a",ge,[t("git-merge"),a(i)]),t(" from Git Docs")]),e("li",null,[e("a",me,[t("git merge"),a(i)]),t(" from Atlassian blog")])]),ue,e("ul",null,[e("li",null,[e("a",pe,[t("Basic Merge Conflicts"),a(i)]),t(" from the Git book")]),e("li",null,[e("a",fe,[t("How Conflicts Are Presented"),a(i)]),t(" from the Git docs")])]),be,e("ul",null,[e("li",null,[e("a",ve,[t("git-revert"),a(i)]),t(" from Git Docs")]),e("li",null,[e("a",we,[t("git revert"),a(i)]),t(" Atlassian tutorial")])]),ye,e("blockquote",null,[e("ul",null,[e("li",null,[e("a",ke,[t("git-reflog"),a(i)])]),e("li",null,[e("a",xe,[t("Rewriting History"),a(i)])]),e("li",null,[e("a",_e,[t("reflog, your safety net"),a(i)])])])]),Te,e("ul",null,[e("li",null,[e("a",Ge,[t("git-reset"),a(i)]),t(" from Git docs")]),e("li",null,[e("a",qe,[t("Reset Demystified"),a(i)]),t(" from Git Blog")]),e("li",null,[e("a",Re,[t("Ancestry References"),a(i)]),t(" from Git Book")])]),Ae,e("ul",null,[Ee,e("li",null,[t("create a repo to track your computer's settings - "),e("a",je,[t("https://dotfiles.github.io/"),a(i)])]),Ie,e("li",null,[t("try tackling some Git challenges with the "),e("a",He,[t("Git-it app"),a(i)])]),e("li",null,[e("a",Be,[t("Udacity Nanodegree Style Guide"),a(i)])])]),Ce])}const Me=n(d,[["render",De],["__file","git.html.vue"]]);export{Me as default};
